# AIS_static_tracker
Python script to access api live stream, store ships in a dictionary and output to json. HTML and CSS take json output and display on a map.

## How to run
Install relevant dependencies listed in pyproject.toml
Copy your api key (find on https://aisstream.io/) into new config.py file, an example file is give.

